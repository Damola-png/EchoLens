streamlit>=1.35
Pillow>=10.0
numpy>=1.26
pandas>=2.0
matplotlib>=3.8

# Optional local backend dependencies for full face matching support.
# These are intentionally excluded from default cloud deploys because
# dlib/face-recognition often fail to build on hosted environments.
# face-recognition>=1.3.0
# opencv-python>=4.9
